ctxeval-1-0 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-1 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-2 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-3 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-4 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-5 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-6 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-7 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-8 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-9 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-10 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-11 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-12 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-13 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-14 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-15 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-16 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-17 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-18 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-19 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-20 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-21 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-22 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-23 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-24 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-25 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-26 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-27 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-28 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-29 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-30 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-31 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-32 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-33 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-34 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-35 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-36 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-37 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-38 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-39 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-40 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-41 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-42 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-43 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-44 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-45 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-46 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-47 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-48 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-49 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-50 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-51 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-52 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-53 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-54 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-55 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-56 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-57 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-58 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-59 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-60 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-61 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-62 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-63 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-64 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-65 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-66 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-67 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-68 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-69 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-70 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-71 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-72 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-73 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-74 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-75 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-76 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-77 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-78 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-79 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-80 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-81 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-82 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-83 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-84 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-85 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-86 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-87 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-88 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-89 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-90 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-91 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-92 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-93 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-94 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-95 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-96 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-97 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-98 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-99 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-100 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-101 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-102 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-103 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-104 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-105 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-106 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-107 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-108 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-109 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-110 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-111 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-112 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-113 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-114 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-115 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-116 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-117 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-118 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-119 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-120 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-121 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-122 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-123 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-124 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-125 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-126 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-127 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-128 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-129 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-130 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-131 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-132 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-133 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-134 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-135 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-136 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-137 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-138 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-139 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-140 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-141 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-142 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-143 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-144 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-145 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-146 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-147 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-148 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-149 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-150 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-151 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-152 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-153 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-154 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-155 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-156 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-157 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-158 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-159 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-160 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-161 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-162 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-163 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-164 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-165 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-166 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-167 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-168 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-169 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-170 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-171 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-172 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-173 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-174 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-175 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-176 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-177 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-178 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-179 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-180 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-181 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-182 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-183 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-184 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-185 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-186 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-187 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-188 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-189 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-190 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-191 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-192 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-193 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-194 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-195 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-196 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-197 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-198 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-199 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-200 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-201 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-202 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-203 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-204 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-205 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-206 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-207 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-208 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-209 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-210 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-211 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-212 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-213 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-214 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-215 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-216 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-217 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-218 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-219 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-220 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-221 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-222 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-223 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-224 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-225 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-226 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-227 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-228 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-229 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-230 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-231 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-232 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-233 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-234 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-235 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-236 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-237 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-238 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-239 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-240 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-241 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-242 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-243 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-244 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-245 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-246 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-247 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-248 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-249 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-250 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-251 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-252 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-253 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-254 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-255 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-256 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-257 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-258 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-259 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-260 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-261 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-262 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-263 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-264 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-265 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-266 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-267 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-268 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-269 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-270 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-271 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-272 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-273 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-274 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-275 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-276 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-277 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-278 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-279 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-280 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-281 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-282 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-283 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-284 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-285 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-286 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-287 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-288 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-289 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-290 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-291 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-292 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-293 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-294 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-295 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-296 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-297 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-298 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-299 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-300 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-301 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-302 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-303 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-304 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-305 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-306 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-307 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-308 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-309 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-310 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-311 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-312 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-313 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-314 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-315 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-316 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-317 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-318 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-319 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-320 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-321 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-322 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-323 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-324 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-325 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-326 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-327 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-328 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-329 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-330 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-331 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-332 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-333 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-334 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-335 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-336 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-337 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-338 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-339 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-340 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-341 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-342 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-343 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-344 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-345 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-346 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-347 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-348 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-349 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-350 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-351 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-352 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-353 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-354 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-355 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-356 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-357 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-358 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-359 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-360 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-361 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-362 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-363 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-364 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-365 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-366 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-367 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-368 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-369 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-370 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-371 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-372 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-373 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-374 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-375 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-376 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-377 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-378 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-379 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-380 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-381 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-382 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-383 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-384 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-385 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-386 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-387 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-388 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-389 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-390 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-391 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-392 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-393 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-394 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-395 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-396 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-397 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-398 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-399 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-400 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-401 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-402 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-403 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-404 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-405 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-406 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-407 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-408 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-409 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-410 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-411 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-412 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-413 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-414 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-415 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-416 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-417 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-418 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-419 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-420 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-421 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-422 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-423 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-424 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-425 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-426 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-427 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-428 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-429 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-430 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-431 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-432 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-433 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-434 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-435 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-436 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-437 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-438 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-439 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-440 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-441 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-442 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-443 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-444 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-445 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-446 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-447 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-448 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-449 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-450 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-451 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-452 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-453 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-454 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-455 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-456 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-457 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-458 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-459 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-460 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-461 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-462 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-463 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-464 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-465 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-1-466 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real