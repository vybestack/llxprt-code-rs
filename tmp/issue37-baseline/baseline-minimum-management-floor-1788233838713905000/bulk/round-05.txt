ctxeval-5-0 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-1 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-2 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-3 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-4 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-5 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-6 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-7 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-8 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-9 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-10 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-11 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-12 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-13 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-14 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-15 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-16 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-17 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-18 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-19 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-20 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-21 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-22 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-23 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-24 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-25 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-26 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-27 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-28 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-29 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-30 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-31 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-32 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-33 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-34 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-35 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-36 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-37 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-38 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-39 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-40 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-41 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-42 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-43 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-44 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-45 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-46 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-47 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-48 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-49 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-50 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-51 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-52 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-53 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-54 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-55 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-56 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-57 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-58 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-59 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-60 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-61 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-62 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-63 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-64 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-65 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-66 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-67 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-68 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-69 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-70 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-71 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-72 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-73 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-74 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-75 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-76 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-77 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-78 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-79 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-80 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-81 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-82 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-83 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-84 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-85 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-86 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-87 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-88 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-89 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-90 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-91 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-92 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-93 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-94 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-95 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-96 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-97 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-98 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-99 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-100 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-101 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-102 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-103 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-104 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-105 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-106 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-107 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-108 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-109 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-110 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-111 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-112 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-113 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-114 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-115 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-116 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-117 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-118 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-119 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-120 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-121 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-122 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-123 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-124 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-125 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-126 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-127 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-128 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-129 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-130 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-131 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-132 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-133 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-134 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-135 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-136 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-137 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-138 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-139 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-140 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-141 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-142 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-143 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-144 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-145 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-5-146 CTXEVAL-BULK-BLOCK determinist