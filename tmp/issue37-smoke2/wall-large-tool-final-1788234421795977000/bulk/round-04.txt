ctxeval-4-0 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-1 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-2 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-3 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-4 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-5 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-6 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-7 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-8 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-9 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-10 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-11 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-12 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-13 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-14 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-15 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-16 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-17 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-18 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-19 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-20 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-21 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-22 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-23 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-24 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-25 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-26 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-27 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-28 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-29 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-30 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-31 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-32 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-33 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-34 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-35 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-36 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-37 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-38 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-39 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-40 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-41 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-42 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-43 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-44 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-45 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-46 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-47 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-48 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-49 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-50 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-51 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-52 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-53 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-54 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-55 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-56 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-57 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-58 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-59 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-60 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-61 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-62 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-63 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-64 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-65 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-66 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-67 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-68 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-69 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-70 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-71 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-72 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-73 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-74 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-75 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-76 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-77 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-78 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-79 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-80 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-81 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-82 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-83 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-84 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-85 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-86 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-87 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-88 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-89 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-90 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-91 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-92 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-93 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-94 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-95 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-96 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-97 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-98 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-99 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-100 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-101 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-102 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-103 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-104 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-105 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-106 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-107 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-108 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-109 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-110 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-111 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-112 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-113 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-114 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-115 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-116 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-117 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-118 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-119 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-120 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-121 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-122 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-123 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-124 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-125 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-126 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-127 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-128 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-129 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-130 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-131 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-132 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-133 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-134 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-135 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-136 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-137 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-138 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-139 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-140 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-141 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-142 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-143 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-144 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-145 CTXEVAL-BULK-BLOCK deterministic tool-output fixture
Each expansion appends a unique per-round, per-block index line so no two admitted
tool results are byte-identical. The context runtime must treat these as distinct
real observations, not as deduplicatable noise.

ctxeval-4-146 CTXEVAL-BULK-BLOCK determinist