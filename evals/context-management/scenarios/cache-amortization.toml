schema_version = 1
id = "cache-amortization"
owner_phase = 4
arm = "feature"
expected_status = "red"
expected_reason_class = "missing-evidence"
accept_any_reason = true

[profile]
name = "ctxeval-loopback"
provider = "openai"
model = "ctxeval-fixture"
context_limit_tokens = 20000
max_output_tokens = 2048

[stimulus]
prompt = "CTXEVAL-TASK-14 Alternate append-only work with prefix rewrites under unknown cache telemetry and account every rewrite in the journal."

[wall]
tool_rounds = 4
tool_output_bytes = 20480
fixture = "tool-output-block.txt"

[assertions]
required_context_artifacts = ["context/rewrite-journal.log"]
